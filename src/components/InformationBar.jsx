import "../assets/styles/InformationBar.css";

const InformationBar = () => {
  return (
    <div className="information-bar">
      Backend may be slow so wait for some time for it to get started
    </div>
  );
};

export default InformationBar;
